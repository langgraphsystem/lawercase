# 📦 Установка зависимостей для Telegram бота

## ⚡ Быстрая установка (РЕКОМЕНДУЕТСЯ)

### Шаг 1: Откройте командную строку (CMD)

**Windows**:
- Нажмите `Win+R`
- Введите `cmd`
- Нажмите `Enter`

### Шаг 2: Перейдите в папку проекта

```cmd
cd d:\Программы\mega_agent_pro_codex_handoff
```

### Шаг 3: Установите все зависимости

```cmd
pip install -r requirements.txt
```

**Время установки**: 3-5 минут

---

## 📋 Что будет установлено

### 🔵 Критичные зависимости (обязательные)

| Пакет | Версия | Для чего |
|-------|--------|----------|
| **python-telegram-bot** | 22.0+ | Работа с Telegram Bot API |
| **openai** | 1.58.0+ | Интеграция с OpenAI GPT |
| **anthropic** | 0.40.0+ | Интеграция с Claude AI |
| **langchain** | 0.2.0-0.4.0 | LLM фреймворк |
| **langgraph** | 0.2.30-0.3.0 | Workflow orchestration |
| **pydantic** | 2.7.0+ | Data validation |
| **structlog** | 24.4.0+ | Structured logging |
| **python-dotenv** | 1.0.1+ | Environment variables |

### 🟢 Важные зависимости (рекомендуемые)

| Пакет | Версия | Для чего |
|-------|--------|----------|
| **httpx** | 0.27.0+ | Async HTTP requests |
| **tenacity** | 9.0.0+ | Retry logic |
| **redis** | 5.0.1+ | Caching (опционально) |
| **fastapi** | 0.110.0+ | API endpoints (опционально) |
| **uvicorn** | 0.29.0+ | ASGI server |

### 🟡 Дополнительные зависимости (опциональные)

| Пакет | Версия | Для чего |
|-------|--------|----------|
| **PyPDF2** | 3.0.1+ | PDF обработка |
| **spacy** | 3.7.2+ | NLP анализ текста |
| **google-generativeai** | 0.8.0+ | Google Gemini API |
| **prometheus-client** | 0.20.0+ | Мониторинг метрик |

---

## 🔧 Альтернативные способы установки

### Способ 1: Минимальная установка (только бот)

Если нужен только Telegram бот без дополнительных функций:

```cmd
pip install python-telegram-bot openai langchain langgraph pydantic structlog python-dotenv httpx tenacity
```

### Способ 2: Установка по категориям

#### Telegram Bot базовые
```cmd
pip install python-telegram-bot>=22.0 python-dotenv>=1.0.1
```

#### LLM провайдеры
```cmd
pip install openai>=1.58.0 anthropic>=0.40.0 google-generativeai>=0.8.0
```

#### LangChain/LangGraph
```cmd
pip install langchain>=0.2.0 langgraph>=0.2.30 typing-extensions>=4.10.0
```

#### Утилиты и логирование
```cmd
pip install structlog>=24.4.0 httpx>=0.27.0 tenacity>=9.0.0 pydantic>=2.7.0
```

### Способ 3: Обновление существующих пакетов

Если пакеты уже установлены, но нужны новые версии:

```cmd
pip install --upgrade -r requirements.txt
```

---

## ✅ Проверка установки

### Проверить что Python установлен

```cmd
python --version
```

**Требуется**: Python 3.11 или выше

Если Python не установлен:
- Скачать с https://www.python.org/downloads/
- Выбрать версию 3.11 или новее
- При установке отметить "Add Python to PATH"

### Проверить что pip работает

```cmd
pip --version
```

### Проверить установленные пакеты

```cmd
pip list | findstr telegram
pip list | findstr openai
pip list | findstr langchain
```

**Должны увидеть**:
```
python-telegram-bot    22.x.x
openai                 1.x.x
langchain              0.x.x
langgraph              0.x.x
```

### Проверить импорты в Python

```cmd
python -c "import telegram; print('Telegram OK')"
python -c "import openai; print('OpenAI OK')"
python -c "import langchain; print('LangChain OK')"
python -c "import structlog; print('Structlog OK')"
```

**Если всё установлено правильно**, увидите:
```
Telegram OK
OpenAI OK
LangChain OK
Structlog OK
```

---

## 🚨 Устранение проблем

### Проблема 1: "pip: command not found"

**Решение**:
```cmd
python -m pip install -r requirements.txt
```

### Проблема 2: "Permission denied" / "Access denied"

**Решение**: Установка с правами администратора
```cmd
pip install --user -r requirements.txt
```

Или запустить CMD **от имени администратора**:
- Правый клик на "Командная строка"
- Выбрать "Запуск от имени администратора"

### Проблема 3: Ошибки компиляции (например, для redis)

**Решение**: Установить только бинарные пакеты
```cmd
pip install --only-binary :all: redis
```

Или пропустить проблемные пакеты:
```cmd
pip install -r requirements.txt --no-deps
pip install python-telegram-bot openai langchain langgraph
```

### Проблема 4: Конфликты версий

**Решение**: Создать виртуальное окружение
```cmd
cd d:\Программы\mega_agent_pro_codex_handoff

REM Создать venv
python -m venv venv

REM Активировать venv
venv\Scripts\activate

REM Установить зависимости
pip install -r requirements.txt
```

После этого всегда сначала активируйте venv:
```cmd
cd d:\Программы\mega_agent_pro_codex_handoff
venv\Scripts\activate
python -m telegram_interface.bot
```

### Проблема 5: Долгая установка / timeout

**Решение**: Использовать зеркало PyPI
```cmd
pip install -r requirements.txt --index-url https://pypi.org/simple
```

Или установить без зависимостей от зависимостей:
```cmd
pip install --no-deps -r requirements.txt
```

### Проблема 6: "No module named 'telegram'"

**Причина**: Установлен не тот пакет

**Решение**: Удалить старый, установить правильный
```cmd
pip uninstall telegram -y
pip uninstall python-telegram-bot -y
pip install python-telegram-bot>=22.0
```

---

## 📝 Проверка после установки

### Запустить бота для проверки

```cmd
cd d:\Программы\mega_agent_pro_codex_handoff
python -m telegram_interface.bot
```

**Если всё установлено правильно**, увидите:
```json
{"event": "telegram.bot.starting", "level": "info"}
{"event": "telegram.bot.running", "level": "info"}
```

**Если есть ошибки импорта**:
```python
ModuleNotFoundError: No module named 'telegram'
```
→ Установите `python-telegram-bot`

```python
ModuleNotFoundError: No module named 'openai'
```
→ Установите `openai`

---

## 🌐 Опциональные компоненты

### Redis (для кэширования)

**Не требуется для базовой работы бота**

Если нужен Redis:

#### Windows:
1. Скачать: https://github.com/microsoftarchive/redis/releases
2. Или использовать WSL: `sudo apt install redis-server`
3. Или использовать Docker: `docker run -d -p 6379:6379 redis`

Без Redis бот будет использовать in-memory кэш.

### PostgreSQL (для базы данных)

**Не требуется для базовой работы бота**

Если нужна база данных:
```cmd
pip install asyncpg psycopg2-binary
```

### Pinecone (для векторного поиска)

**Не требуется для базовой работы бота**

Если нужен векторный поиск:
```cmd
pip install pinecone-client
```

---

## 📊 Размер установки

| Категория | Количество пакетов | Размер на диске |
|-----------|-------------------|-----------------|
| Минимальная установка | ~30 пакетов | ~200 MB |
| Полная установка | ~80 пакетов | ~500 MB |
| С ML моделями (spacy) | ~100 пакетов | ~1.5 GB |

---

## 🔄 Обновление зависимостей

### Проверить устаревшие пакеты

```cmd
pip list --outdated
```

### Обновить все пакеты

```cmd
pip install --upgrade -r requirements.txt
```

### Обновить конкретный пакет

```cmd
pip install --upgrade python-telegram-bot
pip install --upgrade openai
```

---

## 📚 Дополнительная информация

### requirements.txt структура

```
# Core dependencies - основные пакеты
python-dotenv, pydantic, setuptools

# LangChain/LangGraph - агентная система
langchain, langgraph, typing-extensions

# LLM Providers - AI провайдеры
anthropic, openai, google-generativeai

# Telegram Bot - бот фреймворк
python-telegram-bot

# Utilities - утилиты
typer, httpx, tenacity, psutil, aiofiles

# Logging - логирование
structlog

# Caching - кэширование
redis[hiredis]

# FastAPI - веб-сервер (опционально)
fastapi, python-multipart, uvicorn

# Monitoring - мониторинг
prometheus-client, networkx

# Document processing - обработка документов
PyPDF2, img2pdf, pikepdf

# NLP - обработка языка
spacy, dateparser

# Numpy - математика
numpy
```

### Файлы для установки

| Файл | Для чего |
|------|----------|
| `requirements.txt` | Все зависимости для production |
| `requirements-dev.txt` | Дополнительно для разработки (если есть) |
| `requirements-test.txt` | Для тестирования (если есть) |

---

## 🎯 Быстрая справка

### Установить всё
```cmd
cd d:\Программы\mega_agent_pro_codex_handoff
pip install -r requirements.txt
```

### Только для бота
```cmd
pip install python-telegram-bot openai langchain langgraph pydantic structlog python-dotenv
```

### Проверить
```cmd
python -c "import telegram; print('OK')"
python -m telegram_interface.bot
```

### Обновить
```cmd
pip install --upgrade -r requirements.txt
```

---

**Время установки**: 3-5 минут
**Размер**: ~500 MB
**Python**: 3.11 или выше
**Дата**: 1 ноября 2025
