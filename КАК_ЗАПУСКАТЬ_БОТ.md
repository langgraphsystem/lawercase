# 🚀 Как запускать Telegram бота MegaAgent

## 📋 Содержание

1. [Локальный запуск (Windows)](#локальный-запуск-windows)
2. [Railway запуск (Production)](#railway-запуск-production)
3. [Проверка работы бота](#проверка-работы-бота)
4. [Остановка бота](#остановка-бота)
5. [Устранение проблем](#устранение-проблем)

---

## 🖥️ Локальный запуск (Windows)

### ⚡ Быстрый старт (Рекомендуется)

**Просто дважды кликните файл**: `start_bot.bat`

Бот запустится в текущем окне и покажет все логи в реальном времени.

**Для остановки**: Нажмите `Ctrl+C` или закройте окно

---

### 🔄 Запуск в фоне (Background)

**Дважды кликните файл**: `start_bot_background.bat`

Бот запустится в фоновом режиме и продолжит работать после закрытия окна.

**Для остановки**: Дважды кликните `stop_bot.bat`

**Для просмотра логов**:
```cmd
type bot_background.log
```

Или откройте файл `bot_background.log` в любом текстовом редакторе.

---

### 💻 Запуск через командную строку

#### Вариант 1: В текущем окне (видны все логи)

```cmd
cd d:\Программы\mega_agent_pro_codex_handoff
python -m telegram_interface.bot
```

**Остановка**: `Ctrl+C`

#### Вариант 2: В фоне с логированием

```cmd
cd d:\Программы\mega_agent_pro_codex_handoff
python -m telegram_interface.bot > bot_live.log 2>&1 &
```

**Остановка**: `taskkill /F /IM python.exe`

**Просмотр логов**: `type bot_live.log`

---

### 🧹 Очистка кэша перед запуском

Если бот ведёт себя странно или показывает старые ошибки:

```cmd
cd d:\Программы\mega_agent_pro_codex_handoff

REM 1. Остановить все процессы Python
taskkill /F /IM python.exe

REM 2. Очистить кэш
for /d /r . %d in (__pycache__) do @if exist "%d" rd /s /q "%d"

REM 3. Запустить бота
start_bot.bat
```

---

## ☁️ Railway запуск (Production)

### 📱 Метод 1: Railway Dashboard (Самый простой)

1. **Зайти на Railway**:
   - Откройте https://railway.app/dashboard
   - Логин: `brotherslyft@gmail.com`

2. **Выбрать проект**:
   - Workspace: `langgraphsystem`
   - Project: `grand-happiness`

3. **Найти bot service**:
   - В списке сервисов найдите `telegram-bot` (или похожее имя)
   - Кликните на него

4. **Сделать Deploy**:
   - Справа вверху найдите кнопку **"Deploy"** или **"Redeploy"**
   - Нажмите её
   - Дождитесь завершения билда (3-5 минут)

5. **Проверить логи**:
   - На той же странице откройте вкладку **"Logs"**
   - Убедитесь что видите JSON формат логов:
   ```json
   {"event": "telegram.bot.starting", "level": "info"}
   {"event": "telegram.bot.running", "level": "info"}
   ```

### 🖥️ Метод 2: Railway CLI

#### Установка Railway CLI (если ещё не установлен)

```bash
# Windows (через npm)
npm install -g @railway/cli

# Или скачать с https://railway.app/cli
```

#### Подключение к проекту

```bash
# Авторизация
railway login

# Подключение к проекту (интерактивно)
railway link

# Выбрать:
# 1. Workspace: langgraphsystem's Projects
# 2. Project: grand-happiness
```

#### Deploy нового кода

```bash
# Способ 1: Redeploy текущего коммита
railway redeploy

# Способ 2: Deploy из локального кода
railway up

# Способ 3: Принудительный rebuild
railway up --force
```

#### Просмотр логов

```bash
# Последние 50 строк
railway logs --tail 50

# Реального времени (следить за логами)
railway logs --follow

# Поиск ошибок
railway logs --tail 100 | grep -i error
```

#### Проверка статуса

```bash
# Статус сервисов
railway status

# Список переменных окружения
railway variables

# Проверить конкретную переменную
railway variables | grep TELEGRAM_BOT_TOKEN
```

### 🔑 Метод 3: Git Push (Auto-deploy)

Если настроен auto-deploy из GitHub:

```bash
cd d:\Программы\mega_agent_pro_codex_handoff

# Убедиться что на правильной ветке
git branch --show-current
# Должно показать: hardening/roadmap-v1

# Запушить изменения
git push origin hardening/roadmap-v1

# Railway автоматически задеплоит через 1-2 минуты
# Проверить через Railway Dashboard или CLI:
railway logs --follow
```

---

## ✅ Проверка работы бота

### 📱 Тестирование через Telegram

1. **Найти бота**: `@lawercasebot`

2. **Проверить команды**:

   ```
   /start
   ```
   **Ожидаемый ответ**: Приветствие и список команд

   ```
   /help
   ```
   **Ожидаемый ответ**: Подробная справка по командам

   ```
   /ask
   ```
   **Ожидаемый ответ**: "Please provide a question. Usage: /ask <your question>"

   ```
   /ask What is EB-1A visa?
   ```
   **Ожидаемый ответ**: Детальный ответ от MegaAgent

### 🔍 Проверка логов

#### Локальный бот (Windows)

```cmd
REM Если запущен через start_bot_background.bat
type bot_background.log | findstr /i "error"

REM Последние 20 строк
powershell -Command "Get-Content bot_background.log -Tail 20"
```

#### Railway бот

```bash
# CLI
railway logs --tail 50

# Или через Dashboard:
# Railway Dashboard → Project → Service → Logs tab
```

### ✔️ Признаки успешного запуска

**Должны видеть такие логи**:

```json
{"event": "telegram.bot.starting", "level": "info", "timestamp": "..."}
{"event": "telegram.bot.running", "level": "info", "timestamp": "..."}
```

**HTTP запросы должны быть успешными**:

```
HTTP Request: POST https://api.telegram.org/bot.../getMe "HTTP/1.1 200 OK"
HTTP Request: POST https://api.telegram.org/bot.../getUpdates "HTTP/1.1 200 OK"
```

**При получении команды /start**:

```json
{"user_id": 7314014306, "event": "telegram.start.received", "level": "info"}
{"user_id": 7314014306, "event": "telegram.start.sent", "level": "info"}
```

### ❌ Признаки проблем

**Старый формат логов** (устаревший код):
```
[INFO] event="telegram.bot.starting"  ❌ ПЛОХО - старый код
```
**Решение**: Redeploy на Railway или очистить кэш локально

**Ошибки Event loop**:
```
RuntimeError: This event loop is already running
```
**Решение**: Обновить код (должно быть исправлено в commit 129f15e)

**Ошибки AttributeError**:
```
AttributeError: 'RBACManager' object has no attribute 'check_permission'
```
**Решение**: Обновить код (исправлено в последних коммитах)

---

## 🛑 Остановка бота

### Локальный бот

#### Метод 1: Через bat-файл
Дважды кликните `stop_bot.bat`

#### Метод 2: Через командную строку
```cmd
taskkill /F /IM python.exe
```

#### Метод 3: Ctrl+C
Если бот запущен в текущем окне, просто нажмите `Ctrl+C`

### Railway бот

Бот на Railway работает постоянно (24/7). Для остановки:

#### Через Dashboard
1. Railway Dashboard → Project → Service
2. Нажмите "⋯" (три точки) справа вверху
3. Выберите "Stop" или "Disable"

#### Через CLI
```bash
# Остановить сервис
railway service stop

# Или полностью удалить deployment
railway down
```

**⚠️ Внимание**: В production обычно НЕ нужно останавливать бота, он должен работать постоянно.

---

## 🔧 Устранение проблем

### Проблема 1: "Python not found"

**Решение**:
```cmd
REM Проверить установку Python
python --version

REM Если не установлен, скачать с:
REM https://www.python.org/downloads/
REM Версия: Python 3.11 или выше
```

### Проблема 2: "Module 'telegram_interface' not found"

**Решение**:
```cmd
cd d:\Программы\mega_agent_pro_codex_handoff

REM Установить зависимости
pip install -r requirements.txt

REM Или конкретные пакеты
pip install python-telegram-bot openai anthropic
```

### Проблема 3: Бот не отвечает на команды

**Проверка 1**: Логи
```cmd
type bot_background.log | findstr /i "error"
```

**Проверка 2**: TELEGRAM_BOT_TOKEN
```cmd
REM Открыть .env файл
notepad .env

REM Должна быть строка:
REM TELEGRAM_BOT_TOKEN=7472625853:AAGPl30wtI9g57VqYIAO4H2WyXnrZgk4scA  # pragma: allowlist secret
```

**Проверка 3**: Internet connection
```cmd
ping api.telegram.org
```

### Проблема 4: "401 Unauthorized" от OpenAI

**Это нормально!** Если локально не настроен OPENAI_API_KEY.

**Решение для production**:
```bash
# Railway CLI
railway variables set OPENAI_API_KEY=sk-proj-YOUR-KEY-HERE

# Или через Dashboard:
# Railway → Project → Service → Variables → Add
```

### Проблема 5: Старые ошибки после обновления кода

**Причина**: Python кэш (__pycache__)

**Решение**:
```cmd
cd d:\Программы\mega_agent_pro_codex_handoff

REM 1. Остановить бота
taskkill /F /IM python.exe

REM 2. Очистить кэш
for /d /r . %d in (__pycache__) do @if exist "%d" rd /s /q "%d"

REM 3. Перезапустить
start_bot.bat
```

### Проблема 6: Railway показывает старый код

**Причина**: Не сделан redeploy после git push

**Решение**:
```bash
# Проверить последний коммит в GitHub
git log -1 --oneline

# Должно быть: 8dfcd57 или новее

# Сделать redeploy
railway redeploy

# Дождаться завершения (3-5 минут)
railway logs --follow
```

### Проблема 7: "Railway CLI not found"

**Решение**:
```bash
# Установить через npm
npm install -g @railway/cli

# Проверить установку
railway --version

# Или скачать с официального сайта:
# https://railway.app/cli
```

---

## 📚 Дополнительная информация

### Файлы запуска созданы

| Файл | Назначение |
|------|------------|
| `start_bot.bat` | Запуск в текущем окне (видны логи) |
| `start_bot_background.bat` | Запуск в фоне |
| `stop_bot.bat` | Остановка всех процессов бота |

### Полезные команды

```cmd
REM Проверить что бот работает
tasklist | findstr python

REM Посмотреть последние логи
powershell -Command "Get-Content bot_background.log -Tail 30"

REM Следить за логами в реальном времени
powershell -Command "Get-Content bot_background.log -Wait -Tail 10"

REM Очистить все логи
del *.log *.pid
```

### Ссылки на документацию

- **Полный статус исправлений**: `COMPLETE_BOT_FIX_STATUS_2025-11-01.md`
- **Railway deployment guide**: `RAILWAY_GRAND_HAPPINESS_CHECK.md`
- **Telegram Bot API**: https://core.telegram.org/bots/api
- **Railway Docs**: https://docs.railway.app/

### Контакты

- **GitHub**: https://github.com/langgraphsystem/lawercase
- **Ветка**: `hardening/roadmap-v1`
- **Telegram Bot**: `@lawercasebot` (ID: 7472625853)
- **Railway Account**: brotherslyft@gmail.com
- **Railway Project**: grand-happiness

---

## 🎯 Быстрая справка

### Локально (Windows)

```cmd
REM Запуск
start_bot.bat

REM Остановка
Ctrl+C или stop_bot.bat

REM Логи
type bot_background.log
```

### Railway (Production)

```bash
# Запуск/Deploy
railway redeploy

# Логи
railway logs --follow

# Статус
railway status
```

### Тестирование

```
1. Открыть Telegram
2. Найти @lawercasebot
3. Отправить /start
4. Отправить /ask What is EB-1A?
```

---

**Дата создания**: 1 ноября 2025
**Версия**: 1.0
**Статус**: Все 8 ошибок исправлены ✅
**Код готов**: 99.1% ✅
