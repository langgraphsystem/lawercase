# Minimal CI Dependencies - optimized for fast installation
# Uses version ranges instead of pinning to avoid deep resolution conflicts

# Core framework
python-dotenv>=1.0.1,<2.0.0
pydantic>=2.7.0,<2.10.0
pydantic-settings>=2.2.0,<2.6.0

# LangChain ecosystem - let pip resolve compatible versions
langchain>=0.2.0,<0.3.0
langgraph>=0.2.30,<0.3.0

# LLM SDKs
anthropic>=0.40.0,<0.50.0
openai>=1.50.0,<1.60.0
google-generativeai>=0.8.0,<0.9.0

# Core utilities
typing-extensions>=4.10.0,<5.0.0
httpx>=0.27.0,<0.28.0
tenacity>=8.1.0,<9.0.0
orjson>=3.9.0,<3.10.0
python-dateutil>=2.8.0,<3.0.0
dateparser>=1.2.0,<2.0.0

# Testing infrastructure
pytest>=8.0.0,<8.3.0
pytest-asyncio>=0.23.0,<0.24.0
pytest-cov>=5.0.0,<5.1.0
pytest-mock>=3.12.0,<3.15.0
respx>=0.20.0,<0.21.0

# FastAPI for API tests
fastapi>=0.110.0,<0.115.0
uvicorn>=0.29.0,<0.31.0
python-multipart>=0.0.6,<0.1.0

# Redis mocking
redis>=5.0.0,<5.1.0
fakeredis>=2.23.0,<2.25.0

# Additional test dependencies
numpy>=1.26.0,<1.27.0
freezegun>=1.5.0,<1.6.0

# Minimal observability
structlog>=24.4.0,<24.5.0

# Security & Authentication (for API tests)
python-jose[cryptography]>=3.3.0,<4.0.0

# Optional: Telegram bot (for telegram tests)
# python-telegram-bot>=21.4,<22.0.0  # Commented out - heavy dependency

# Optional: SpaCy (for NLP tests)
# spacy>=3.7.2,<4.0.0  # Commented out - very heavy dependency (~100MB)
