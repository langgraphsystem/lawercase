# Nixpacks configuration for Railway deployment
# Bot runs via webhook through FastAPI, not standalone polling

[start]
cmd = "python -m uvicorn --app-dir . api.main:app --host 0.0.0.0 --port ${PORT}"
