[build]
builder = "NIXPACKS"

[deploy]
startCommand = "sh -c 'cd /app && python -m uvicorn api.main:app --host 0.0.0.0 --port $PORT'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
