[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
buildTarget = "bot"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[[services]]
name = "megaagent-api"

[services.environment]
ENV = "production"
PYTHONUNBUFFERED = "1"

[[services]]
name = "megaagent-bot"

[services.build]
buildTarget = "bot"

[services.deploy]
startCommand = "python -m telegram_interface.bot"
